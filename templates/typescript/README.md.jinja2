# {{ api_name }} SDK

TypeScript SDK for {{ api_name }} - Generated by API SDK Generator

## Installation

```bash
npm install {{ package_name }}
```

## Quick Start

```typescript
import { {{ api_name_class }} } from '{{ package_name }}';

const client = new {{ api_name_class }}({
  {% if auth_type == 'api_key' %}apiKey: 'your-api-key',{% endif %}
  {% if auth_type == 'bearer' %}bearerToken: 'your-bearer-token',{% endif %}
  {% if auth_type == 'basic' %}username: 'your-username',
  password: 'your-password',{% endif %}
  baseURL: '{{ base_url }}'
});

// Example usage will be added here
```

## Authentication

{% if auth_type == 'api_key' %}
This SDK uses API Key authentication. Provide your API key when initializing the client:

```typescript
const client = new {{ api_name_class }}({
  apiKey: 'your-api-key'
});
```
{% elif auth_type == 'bearer' %}
This SDK uses Bearer Token authentication. Provide your bearer token when initializing the client:

```typescript
const client = new {{ api_name_class }}({
  bearerToken: 'your-bearer-token'
});
```
{% elif auth_type == 'oauth2' %}
This SDK uses OAuth2 authentication. Provide your access token when initializing the client:

```typescript
const client = new {{ api_name_class }}({
  accessToken: 'your-access-token'
});
```
{% elif auth_type == 'basic' %}
This SDK uses Basic authentication. Provide your username and password when initializing the client:

```typescript
const client = new {{ api_name_class }}({
  username: 'your-username',
  password: 'your-password'
});
```
{% endif %}

## Configuration Options

```typescript
interface SDKConfig {
  {% if auth_type == 'api_key' %}apiKey?: string;{% endif %}
  {% if auth_type == 'bearer' %}bearerToken?: string;{% endif %}
  {% if auth_type == 'basic' %}username?: string;
  password?: string;{% endif %}
  baseURL: string;
  timeout?: number; // Request timeout in milliseconds (default: 30000)
  {% if enable_retry_logic %}retryConfig?: RetryConfig; // Retry configuration{% endif %}
  {% if enable_rate_limiting %}rateLimitConfig?: RateLimitConfig; // Rate limiting configuration{% endif %}
  logLevel?: 'debug' | 'info' | 'warn' | 'error'; // Logging level (default: 'info')
}
```

{% if enable_retry_logic %}
### Retry Configuration

The SDK includes automatic retry logic with exponential backoff:

```typescript
const client = new {{ api_name_class }}({
  apiKey: 'your-api-key',
  retryConfig: {
    maxRetries: 3,
    baseDelay: 1000, // milliseconds
    maxDelay: 30000, // milliseconds
    retryableStatusCodes: [408, 429, 500, 502, 503, 504]
  }
});
```
{% endif %}

{% if enable_rate_limiting %}
### Rate Limiting

The SDK includes built-in rate limiting to respect API quotas:

```typescript
const client = new {{ api_name_class }}({
  apiKey: 'your-api-key',
  rateLimitConfig: {
    requestsPerSecond: 10,
    burstAllowance: 5
  }
});
```
{% endif %}

## Error Handling

The SDK provides typed error classes for different scenarios:

```typescript
import { 
  APIError, 
  NetworkError, 
  RateLimitError, 
  AuthenticationError,
  ValidationError,
  NotFoundError,
  ServerError
} from '{{ package_name }}';

try {
  const result = await client.someMethod();
} catch (error) {
  if (error instanceof RateLimitError) {
    console.log(`Rate limited. Retry after ${error.retryAfter} seconds`);
  } else if (error instanceof AuthenticationError) {
    console.log('Authentication failed. Check your API key.');
  } else if (error instanceof NetworkError) {
    console.log('Network error. Check your connection.');
  } else {
    console.log('An error occurred:', error.message);
  }
}
```

## API Methods

<!-- LLM-generated usage examples will be inserted here -->

## TypeScript Types

The SDK is fully typed. Import types as needed:

```typescript
import type { 
  // Type definitions will be listed here
} from '{{ package_name }}';
```

## License

{{ license }}

---

**Generated by [API SDK Generator](https://github.com/yourusername/api-sdk-generator)**
